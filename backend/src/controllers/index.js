const {getProducts} = require("./product");
const {filterProducts} = require("./product");
const userController = require("./user");
// const userController = require("./user");

module.exports= {getProducts, filterProducts, userController};